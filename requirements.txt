# gprStudio dependencies — used by Streamlit Community Cloud and local installs.
#
# The vendored gprMax (vendor/gprMax) is installed FROM SOURCE so its Cython /
# OpenMP extensions are compiled for the target platform (the committed .pyd are
# Windows-only). This needs a C compiler: build-essential on Linux (declared in
# packages.txt, which is what Streamlit Community Cloud uses) or the MSVC Build
# Tools on Windows. Installing it also pulls gprMax's own runtime dependencies
# (numpy, scipy, h5py, matplotlib, colorama, psutil, terminaltables, tqdm).
streamlit>=1.34
streamlit-image-coordinates>=0.4.0
./vendor/gprMax
